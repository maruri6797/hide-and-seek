<div class="container">
  <div class="row">
    <div class="col-lg-12 col-md-10">
      <% if @notifications.exists? && @notifications.visitor_id != current_user.id %>
        <%= render 'public/notifications/index' %>
      <% else %>
        <p>通知はありません</p>
      <% end %>
    </div>
  </div>
</div>
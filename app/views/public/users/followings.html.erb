<div class="container">
  <div class="row">
    <div class="col-lg-10 col-md-8">
      <h3>フォロー中</h3>
      <% if @users.present? %>
        <%= render 'public/users/user', users: @users %>
      <% else %>
        <p>フォロー中のユーザーはいません</p>
      <% end %>
    </div>
  </div>
</div>
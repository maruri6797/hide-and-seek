<div class="container">
    <div class="row">
    <div class="col-lg-10 col-md-8">
      <h3>メッセージを送る</h3>
      <% if @users.present? %>
        <%= render 'public/users/user', users: @users %>
      <% else %>
        フォロー中のユーザーがいません
      <% end %>
    </div>
  </div>
</div>